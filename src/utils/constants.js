const defaultBreakTime = 300;
const defaultSessionTime = 1500;
const minTime = 60;
const maxTime = 3600;
const interval = 60;

export { defaultBreakTime, defaultSessionTime, minTime, maxTime, interval };
